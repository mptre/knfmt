int
main(void)
{
	int prev = -1;
	int cnt, fn_nid, fn_opt, i, len, orflags, outlen, sep_dn_len,
	    sep_eq_len, sep_mv_len;
	ASN1_OBJECT *fn;
	ASN1_STRING *val;
	X509_NAME_ENTRY *ent;
	char objtmp[80];
	char *sep_dn, *sep_eq, *sep_mv;
	const char *objbuf;

	int		 prev = -1;
	int		 cnt, fn_nid, fn_opt, i, len, orflags, outlen,
			 sep_dn_len, sep_eq_len, sep_mv_len;
	ASN1_OBJECT	*fn;
	char		*sep_dn, *sep_eq, *sep_mv;
}
